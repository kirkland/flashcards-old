<h1>Editing deck</h1>

<% form_for(@deck) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'fields', :locals => { :f => f } %>
  <p><%= f.submit "Update" %></p>
<% end %>

<%= link_to 'Show', deck_path(@deck) %> |
<%= link_to 'Back', decks_path %>
