<h1>New deck</h1>

<% form_for(@deck) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'fields', :locals => { :f => f } %>
  <p><%= f.submit "Create" %></p>
<% end %>

<%= link_to 'Back', decks_path %>
