<h1>Users</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% for user in @users %>
  <tr>
    <td><%=h user.username %></td>
    <td><%= link_to 'View Decks', user_decks_path(user) %></td>
  </tr>
<% end %>
</table>

<br />
